As you see, I'm trying to implement a uniformed interface for different simulation to do fast sim2sim switch. I now have implemented and tested genesis. Now I want to have a 2D simulation (to keep API the same, still keep the 3D dim, just set the z dim to all 0) Then, please use pymunk to build a 2D interface , follow the function organization as genesis_interface One thing to keep in mind is that, pymunk does not support vectorization by default, but our interface is vectorized, to do this, you need to do multiprocessing for n_envs number of pymunk simulations
Since it's 2D, we cannot simulate the 3D joints, so actually we just use the bound box as the robot's 2D shape. All the entity methods should be all avaible, while for the robot, those joint methods just raise not implemeneted error
Remeber, don't touch debugmark manager and camera manager, those are not simulator specific code, all simulator specific code should be included in mymunk interface

So for each robot/entity , we need to have body(s) + shape(s), let's combine those two as a PymunkEntityData, which correspond to genesis's entity class (We need a class to store everything we need to use / interact with the entity)

Please include the reference to pymunk space inside PymunkEntityData, so that we only need the one PymunkEntityData object to create an PymunkEntityInterface. Second thing is that, we need a PymunkEntityData class that can store multiple bodies + multiple shapes for each env. I think we need a 2D array to store n_envs* n_bodies and an array to store n_envs*n_shapes for body and shape instances. each shape and body have an index , then we use a dict to map from body to shapes the body have(both idx). And, a list of Joint instance, and a list of Motor instance. Plus a 2D table to store the body <-> body relationship, each grid have (Joint idx, Moter idx(might be None)) Tuple. And, the [0] body would be considered as root body, In our case, we might not use these functionality as now we are creating from the bounding box, but having this would be better

Also for all field, I always prefer @property to things like self._device = config.device self._n_envs = config.n_envs self._dt = config.dt self._substeps = config.substeps self._gravity = config.gravity. Because this might cause sync problem. Just store the config instance and use @property to quick access to fields that is already provided by  SimulatorConfig

Which use pd to control the base pos / vel. Since pymunk don't have built in pd support, we need to do that in our layer, the PymunkEntityData should contains a list that pd values that  will be automatically applied when we are trying to "control" joint or base. The shape of the pd is 3 + number of motors  , the first 3 are for base, which is x, y, yaw, others are for Motors.